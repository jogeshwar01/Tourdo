doctype html
html
  head
    //- these assets will be looked in public folder as we specified it in the views path in app.js
    block head
      meta(charset='UTF-8')
      meta(name='viewport' content='width=device-width, initial-scale=1.0')
      link(rel='stylesheet' href='/css/style.css')
      link(rel='shortcut icon' type='image/png' href='/img/favicon.png')
      link(rel='stylesheet' href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
      title Natours | #{title}

  body
    // HEADER
    include _header

    // CONTENT
    block content
      h1 This will be overwritten when some template extends this file 

    // FOOTER
    include _footer

    //- script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/0.24.0/axios.min.js')
    //- script(src='/js/mapbox.js')
    //- script(src='/js/login.js')
    script(src='/js/bundle.js')

    //-his needs to be at end of base.pug as DOM may not be already loaded if we put it before

    //- unbuffered code -wont be added to output
    //-  // just 2 slashes--this will be a html comment and not for this file